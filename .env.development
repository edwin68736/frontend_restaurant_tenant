# Desarrollo: npm run dev | npm run tauri:dev
# Backend local:
VITE_API_URL=http://localhost:3000
# Probar contra API de producción (Vite hace proxy /api → sin CORS en el navegador):
# VITE_API_URL=https://api.tukifac.com