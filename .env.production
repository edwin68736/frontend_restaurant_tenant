# Build Tauri / release — solo API CENTRAL para consulta RUC (bootstrap).
# Tras el RUC la app guarda api_url del tenant en disco y todas las peticiones van ahí.
VITE_CENTRAL_API_URL=https://api.tukifac.com
