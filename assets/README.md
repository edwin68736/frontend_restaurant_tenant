# Assets nativos (Capacitor)

Los PNG de esta carpeta se generan con:

```bash
npm run icons:generate
```

**Fuente:** `public/logo-tukichef.png`

No edites `icon-only.png` ni `splash.png` a mano; vuelve a ejecutar el script si cambias el logo.
